# Repository instructions

- This is a static, zero-setup training site. Preserve direct browser use and avoid adding a build system unless explicitly requested.
- Preview locally with `python -m http.server 8000` and verify changed pages in a browser at desktop and mobile widths.
- Keep exercises educational and defensive. Never include real credentials, private targets, or instructions that act on systems without authorization.